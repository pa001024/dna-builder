[package]
name = "hpatchz-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[build-dependencies]
cc = "1"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
codegen-units = 1
